namespace Akila.FPSFramework
{
    public enum MessageLevel
    {
        Maximum = 0,   // Most detailed, for developers
        Normal = 1,    // General information
        Minimal = 2,   // Minimal, only important notices
        Silent = 3     // None, suppress all messages
    }
}
